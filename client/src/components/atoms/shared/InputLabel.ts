import styled from 'styled-components'
import tw from 'twin.macro'
import twcolors from 'shared/colors/twcolors'

export const InputLabel = styled.label`
	${tw`block text-sm font-bold mb-2`}
	${twcolors.TEXT_GRAY}
`
